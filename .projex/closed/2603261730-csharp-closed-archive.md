# Archive: csharp-closed

> **Created:** 2026-03-26
> **Scope:** `csharp/.projex/closed/`
> **Files Archived:** 70

---

## Summary

This index covers closed C# runtime projex from late February through late March 2026: expression interpolation and special forms, `Context`/`ZohRuntime` execution API, two-phase continuations, statement state, presentation and diagnostic alignment, core verb namespace reorg, and Phase 4/5 control-flow and navigation fixes. Work is mostly captured as plan / log / walkthrough triplets with test counts noted in execution records.

---

## Index

| Filename | Summary | Keywords |
|----------|---------|----------|
| `20260225-string-interpolation-formatting-plan-walkthrough.md` | Post-implementation walkthrough for C#-style `,width` and `:format` on `${*var}` interpolation via `ConsumedTokensCount`, `ExpressionEvaluator`, and expanded `ExpressionTests`; notes review-driven extra coverage. | walkthrough, interpolation, formatting, ExpressionParser, ExpressionEvaluator, string.Format, InvariantCulture, tests, complete |
| `20260225-string-interpolation-formatting-plan.md` | Plan to split interpolation bodies so expressions parse cleanly then apply composite format specifiers per spec audit; covers parser token exposure, evaluator suffix handling, mixed syntax, and diagnostics. | plan, interpolation, composite format, ExpressionParser, ExpressionEvaluator, spec audit, diagnostics, scanner suffix |
| `20260226-runtime-run-context-align-log.md` | Execution log for adding `StatementExecutor` and `Context.Run()`, slimming `ZohRuntime.Run` to `Run(ctx)`, and mechanically updating test call sites including a second pass for `context` variable names. | execution log, Context, ZohRuntime, StatementExecutor, Run, refactor, tests, dotnet |
| `20260226-runtime-run-context-align-plan.md` | Plan to remove redundant `CompiledStory` from `Run`, move the execution loop onto `Context` per runtime spec, and leave `ZohRuntime.Run` as a thin delegate. | plan, spec alignment, Context, ZohRuntime, execution loop, CompiledStory, CreateContext, tests |
| `20260226-runtime-run-context-align-walkthrough.md` | Walkthrough confirming loop extraction, `StatementExecutor` wiring, one-line `ZohRuntime.Run(ctx)`, and test updates with minor `Clone()` propagation called out. | walkthrough, Context.Run, StatementExecutor, ZohRuntime, refactor, spec, tests, complete |
| `20260226-runtime-run-to-completion-plan.md` | Plan to add `ZohRuntime.RunToCompletion(Context)` that runs synchronously and returns `LastResult`, covering termination and continuation or blocking edge cases with tests. | plan, RunToCompletion, ZohRuntime, synchronous, LastResult, Context, spec, RuntimeTests |
| `20260226-runtime-run-to-completion-shorthand-plan.md` | Plan for `RunToCompletion(string)` that chains load, create context, and run and returns the finished `Context` for inspection rather than only a value. | plan, RunToCompletion, shorthand, LoadStory, CreateContext, embedder ergonomics, Context, tests |
| `20260227-phase4-control-flow-gaps-fix-plan.md` | Umbrella Phase 4 audit plan reconciled and closed via child work on `/if`, `/switch`, `/foreach`, `breakif` or `continueif`, `/do` second hop, plus follow-up condition suspend or fatal alignment. | phase 4, control flow, drivers, spec audit, if, switch, foreach, breakif, do, complete |
| `20260227-phase5-concurrency-context-signal-gaps-fix-plan.md` | Narrow Phase 5 plan delivering trailing `*var` transfer on `/jump` and `/fork` before contract validation, aligned with `CallDriver` scope copy; records earlier `/call`, `/flag`, `/wait` as already done. | phase 5, jump, fork, variable transfer, CallDriver, TryGetWithScope, concurrency, spec audit, complete |
| `20260227-runtime-run-to-completion-patch.md` | Patch adding `RunToCompletion(Context)` after `Run(ctx)` returning `LastResult`, with tests adjusted because `/set` yields nothing and `/get` supplies a return value. | patch, RunToCompletion, ZohRuntime, LastResult, RuntimeTests, spec, complete |
| `20260227-runtime-run-to-completion-shorthand-patch.md` | Patch adding `RunToCompletion(string)` that returns the post-run `Context` and tests for executed scripts and empty stories. | patch, RunToCompletion, string overload, Context, ZohRuntime, RuntimeTests, overload resolution |
| `20260228-expression-special-forms-spec-parity-followup-patch.md` | Patch enforcing spec on bare `$(...)` without suffix and ternary `:` in `$?(...)`; updates parser and tests with automated full run skipped per user partial success. | patch, ExpressionParser, special forms, ternary, colon, spec parity, partial success, tests |
| `20260228-expression-special-forms-spec-parity-followup-plan.md` | Plan to close parser divergence from `spec/expr.md` for dollar-paren suffix rules and ternary separator, updating parser compliance and evaluation tests while preserving any-form `$?(a|b|c)`. | plan, expression parser, dollar paren, conditional, ternary, expr.md, tests, compliance |
| `20260228-rng-parse-gaps-fix-log.md` | Execution log for `/wroll` weight validation fatals and `/parse` list or map via `System.Text.Json`, with targeted then full suite verification and noted git workflow deviations. | execution log, wroll, parse, RollDriver, ParseDriver, JSON, audit gap, tests |
| `20260228-rng-parse-gaps-fix-plan.md` | Plan for audit gaps 3.4: replace silent negative weight clamping in `/wroll` with diagnostics and implement JSON list and map parsing in `/parse` with recursive `JsonElement` to `ZohValue`. | plan, wroll, parse, RollDriver, ParseDriver, invalid_value, JSON, ZohList, ZohMap, audit |
| `20260301-two-phase-continuation-csharp-impl-log.md` | Execution log for migrating the C# runtime from `VerbResult`/`VerbContinuation` to `DriverResult`, `Continuation`, `WaitRequest`, and `WaitOutcome`, including Context `ApplyResult`/`Resume`, all blocking drivers, SignalManager, and test updates; build and 605 tests passed. | continuation, DriverResult, Context, IVerbDriver, blocking drivers, SignalManager, execution log, C# runtime |
| `20260301-two-phase-continuation-csharp-impl-plan.md` | Plan to implement the two-phase continuation model: fix IP advancement and resume callbacks by replacing legacy result/continuation types and routing `SignalManager` through token-guarded `Resume`. | two-phase continuation, DriverResult, WaitRequest, WaitOutcome, Context, spec parity, C# runtime, blocking verbs |
| `20260301-two-phase-continuation-csharp-impl-walkthrough.md` | Post-execution walkthrough confirming the full migration across runtime and tests, `ApplyResult` IP rules, token-guarded resume, and driver-owned `onFulfilled` closures with all tests green. | walkthrough, continuation, DriverResult, Context, Resume, ApplyResult, migration, verification, success |
| `20260303-interpolation-conditional-syntax-csharp-log.md` | Log of implementing native `$?{...}` / `$#{...}` parsing with format suffixes via `ParseInterpolationConditionalOrAny` and refactored `EvaluateInterpolationMatch`, plus ExpressionTests and edge-case formatting tests. | interpolation, ExpressionParser, ExpressionEvaluator, ternary, format suffix, ExpressionTests, execution log, C# |
| `20260303-interpolation-conditional-syntax-csharp-plan.md` | Plan to fix `$?{cond ? A : B, width:format}` by parsing conditional/Any without bogus `$?(...)` wrapping so trailing commas no longer break the parser. | interpolation, conditional syntax, ExpressionParser, formatting, string interpolation, bugfix, C# plan, spec alignment |
| `20260303-interpolation-conditional-syntax-csharp-walkthrough.md` | Walkthrough of the interpolation refactor: direct lex/parse per open token, new parser helper, native tests replacing nested workarounds, ExpressionTests passing. | walkthrough, interpolation, ParseInterpolationConditionalOrAny, EvaluateInterpolationMatch, formatting, parser, tests |
| `20260305-runtime-api-surface-alignment-log.md` | Execution log for new public API types (`ContextHandle`, `ExecutionResult`, `VariableAccessor`, `WaitConditionState`), `ZohRuntime` tick/resume surface, presentation handlers on handles, `ApiSurfaceTests`, and a sleep-duration fix in one test. | API surface, ZohRuntime, Tick, Resume, ContextHandle, execution log, presentation handlers, tests |
| `20260305-runtime-api-surface-alignment-plan.md` | Plan to align `Zoh.Runtime` with revised runtime spec: handles, tick scheduler, host `Resume`, typed wait conditions, elapsed-time injection instead of wall clock, and obsolete legacy entry points. | runtime API, ContextHandle, ExecutionResult, Tick, scheduler, spec alignment, VariableAccessor, WaitConditionState |
| `20260305-runtime-api-surface-alignment-walkthrough.md` | Walkthrough confirming all plan steps: new types, `StartContext`/`Tick`/`Resume`/`GetResult`, `ContextHandle` for presentation handlers, typed waits, obsolete old API, 616 tests pass. | walkthrough, API alignment, ZohRuntime, ContextHandle, Tick, tests, success, C# runtime |
| `20260305-statement-state-impl-plan-log.md` | Short log adding `StatementState` to `Context` with clearing on complete/terminate/exit story and updating the statement-cache staging eval Q2 resolution. | StatementState, Context, ApplyResult, execution log, eval update, per-statement state |
| `20260305-statement-state-impl-plan.md` | Plan to add spec-mirrored `StatementState` on `Context`, clear it on complete paths (not on suspend), and update related eval open questions. | StatementState, Context, dictionary, spec parity, clearing semantics, statement cache, C# |
| `20260305-statement-state-impl-walkthrough.md` | Walkthrough of implementing `StatementState` lifecycle clearing and resolving eval Q2 about outcome delivery via `onFulfilled` rather than a separate reinvoke. | walkthrough, StatementState, Context, staging, eval, tests, success |
| `20260306-inline-call-handle-join-plan.md` | Plan (Option A) for handle-backed `JoinContextRequest`/`ContextJoinCondition`, resolve-wait without ID scans, `/call` transfer refs and `[inline]` variable copyback, and guaranteed handles for scheduled contexts. | call, inline, ContextHandle, JoinContextRequest, variable copy, concurrency, CallDriver, ZohRuntime |
| `20260306-inline-call-handle-join-walkthrough.md` | Walkthrough confirming handle-based joins, `ResolveWait` on handle state, `CallDriver` transfer/`[inline]` copyback with scope preservation, `TryGetWithScope`, and regression tests. | walkthrough, call, inline, ContextHandle, join, VariableStore, tests, success |
| `20260315-flow-verbs-test-coverage-plan.md` | Plan to extend Flow verb tests for `/loop`, `/if`, `/while`, `/sequence` and align `IfDriver`/`WhileDriver` with strict boolean/nothing condition checks per `07_control_flow.md`. | flow verbs, unit tests, IfDriver, WhileDriver, strict boolean, control flow, spec alignment, FlowTests |
| `20260315-flow-verbs-test-coverage-walkthrough.md` | Post-execution walkthrough: aligned IfDriver/WhileDriver with strict boolean/nothing checks for implicit `is: true`, added `FlowErrorTests.cs` with 22 tests for `/loop`, `/if`, `/while`, `/sequence`, and verified full suite (639 tests). | flow verbs, test coverage, IfDriver, WhileDriver, invalid_type, parameter_not_found, FlowErrorTests, strict boolean |
| `20260316-embed-variable-interpolation-impl-plan.md` | Plan to extend `EmbedPreprocessor` with `${name}` path resolution (built-ins, runtime flags, metadata order), optional `#embed?` on missing files, and early metadata for embed—scoped out `#strsub` and macro interpolation. | embed, preprocessor, interpolation, embed?, MetadataExtractor, PreprocessorContext, runtime flags, spec parity |
| `20260316-embed-variable-interpolation-impl-walkthrough.md` | Walkthrough: shipped `${name}` and `#embed?`; inlined metadata extraction in `EmbedPreprocessor`, extended `PreprocessorContext` on `IPreprocessor.cs`, 654 tests pass. | EmbedPreprocessor, interpolation, optional embed, metadata, RuntimeFlags, walkthrough, deviation, tests |
| `20260316-presentation-verb-diagnostics-alignment-plan-log.md` | Short execution log (2026-03-20): steps for ChooseDriver, ConverseDriver, PromptDriver, ChooseFromDriver—visibility/empty choices/resume outcomes and test updates. | execution log, presentation verbs, ChooseDriver, ConverseDriver, WaitTimedOut, WaitCancelled, no_choices |
| `20260316-presentation-verb-diagnostics-alignment-plan.md` | Plan to align four presentation drivers with two-phase suspension: execute verb values for visibility, warn on empty choices, differentiate timeout vs cancel on resume; timeout≤0 out of scope. | presentation verbs, diagnostics, suspension, ChooseDriver, ConverseDriver, PromptDriver, ChooseFromDriver, spec alignment |
| `20260316-presentation-verb-diagnostics-alignment-walkthrough.md` | Walkthrough summarizing driver changes and extensive test rewrites; timeouts as INFO `timeout`, cancel as ERROR, empty choices WARNING `no_choices`; 676 tests passed. | walkthrough, presentation, diagnostics, timeout, cancellation, no_choices, tests, two-phase |
| `20260316-runtime-scoped-flags-impl-log.md` | Step-by-step implementation log for runtime/context flags, `/flag`, preprocessor wiring, fork/call copy; Step 7 tests added but `dotnet test` blocked by NU1301 NuGet restore. | execution log, flags, FlagDriver, PreprocessorContext, fork, call, NU1301, partial |
| `20260316-runtime-scoped-flags-impl-plan.md` | Plan for dual-scope flags on `ZohRuntime`/`Context`, `/flag` with optional runtime scope, resolution chain, and passing runtime flags to preprocessors; embed interpolation listed as dependent work. | flags, ZohRuntime, Context, FlagDriver, preprocessor, scope, resolution, spec 09_runtime |
| `20260316-runtime-scoped-flags-impl-walkthrough.md` | Walkthrough: flags and `/flag` implemented, `IExecutionContext` surface, preprocessor `RuntimeFlags`, fork/call inheritance; full suite unverified locally due to blocked restore—partial success. | walkthrough, flags, VerbRegistry, IExecutionContext, ForkDriver, CallDriver, tests blocked, deviation |
| `20260316-tag-attribute-handler-support-patch.md` | Patch note: nullable `Tag` on presentation/media request records, drivers pass `[tag]`, tests for show/play/converse/choose/chooseFrom/prompt; partial success, `dotnet` missing in environment. | tag attribute, handlers, request records, ShowDriver, PlayDriver, patch, tests, partial |
| `20260316-tag-attribute-handler-support-plan.md` | Plan to surface `[tag]` from drivers into handler requests for logging/analytics; nullable trailing `Tag` for backward compatibility; out of scope validation and non-presentation verbs. | tag, std_attributes, handler requests, presentation, media, driver passthrough, nullable |
| `20260316-try-suspension-wrapping-impl-plan-walkthrough.md` | Walkthrough: refactored `TryDriver` with `HandleTryResult` wrapping suspend continuations recursively, catch-on-suspend guard, 11 new tests, 665 tests pass. | TryDriver, suspension, continuation, HandleTryResult, catch, suppress, tests, success |
| `20260316-try-suspension-wrapping-impl-plan.md` | Plan to fix `TryDriver` so post-resume fatals go through try catch/suppress/downgrade per `handleTryResult` spec; scope `TryDriver.cs` only. | TryDriver, suspension, wrapping, fatal, resume, control flow, spec 07, HandleTryResult |
| `202603201757-diagnostic-code-invalid-params-log.md` | Execution log: replaced `arg_count` with `invalid_params` in flow/signal drivers, `no_choices`→`invalid_params` and `type_error`→`invalid_type` in presentation drivers, updated tests, added six missing-arg tests; 682 passed. | invalid_params, arg_count, no_choices, invalid_type, drivers, tests, execution log |
| `202603201757-diagnostic-code-invalid-params-plan.md` | Plan to standardize ad hoc codes to `invalid_params` across listed drivers, update assertions, add tests for previously untested arg-count paths; bonus `invalid_type` in ChooseFromDriver. | invalid_params, diagnostics, ChooseDriver, JumpDriver, ForkDriver, CallDriver, consistency, tests |
| `202603201757-diagnostic-code-invalid-params-walkthrough.md` | Post-execution walkthrough: replaced `arg_count`/`no_choices`/`type_error` with `invalid_params`/`invalid_type` across 8 drivers, fixed ChooseFrom severity, updated 2 tests and added 6; 682 tests passed. | diagnostics, invalid_params, invalid_type, drivers, tests |
| `2603201730-diagnostic-code-invalid-arg-cleanup-plan.md` | Plan to replace ad-hoc `invalid_arg`→`invalid_type` and `invalid_args`→`invalid_params` in 7 verb drivers (13 sites) and fix RollDriver severity Error→Fatal. | invalid_arg, invalid_type, invalid_params, RollDriver, spec alignment |
| `2603201730-diagnostic-code-invalid-arg-cleanup-walkthrough.md` | Short execution summary: same replacements across Jump/Fork/Call/Sleep/Wait/Signal/Roll drivers; 704 tests pass; grep confirms no `invalid_arg`/`invalid_args` in src. | walkthrough, diagnostics, drivers, verification |
| `2603241530-core-verb-namespace-alignment-log.md` | Execution log: verb folder renames, `core.{group}` namespaces, VerbRegistry and parser sugar fixes (`core.var`/`core.nav`/`core.eval`); 704 tests; deviations noted. | namespace, VerbRegistry, Parser, git mv, worktree |
| `2603241530-core-verb-namespace-alignment-plan.md` | Plan to align runtime verb directories and `IVerbDriver.Namespace` with spec `Core.{Group}.{Name}` (Var, Nav, Signal, Channel, etc.); registry and tests; std verbs unchanged. | core.var, restructure, spec, suffix resolution |
| `2603241530-core-verb-namespace-alignment-walkthrough.md` | Close-out: 69-file reorg, registry + parser alignment for desugared AST namespaces; 704 tests; key insight: desugar output must match registered namespaces. | walkthrough, Parser, validation, success |
| `2603251600-phase4-if-verb-subject-else-log.md` | Log: IfDriver executes verb subject before `is:`/default guard, named `else` + positional fallback; three FlowTests; full suite 707. | IfDriver, ExecuteVerb, else, FlowTests |
| `2603251600-phase4-if-verb-subject-else-plan.md` | Plan for `/if` spec parity: run verb subject before boolean guard/compare; support `else:` named param with third positional fallback; tests in FlowTests. | phase4, if, ZohVerb, invalid_type |
| `2603251600-phase4-if-verb-subject-else-walkthrough.md` | Walkthrough confirming IfDriver + three tests; optional main “In Progress” commit skipped; 707 tests; notes on named vs positional `else`. | walkthrough, control flow, regression |
| `2603251601-phase4-switch-verb-case-log.md` | Log: SwitchDriver executes verb case operands before compare; `Switch_EvaluatesVerbCaseValues`; worktree script workaround; 708 tests. | SwitchDriver, ZohVerb, FlowTests, worktree |
| `2603251601-phase4-switch-verb-case-plan.md` | Plan: `/switch` case values that are `ZohVerb` must be executed and return value used in equality compare; one focused FlowTests regression. | switch, phase4, ExecuteVerb |
| `2603251601-phase4-switch-verb-case-walkthrough.md` | Walkthrough: case-side verb execution mirrors subject handling; ReturnStrDriver helper; 708 tests; deviation: manual `git worktree add`. | walkthrough, SwitchDriver, Suspend propagation |
| `2603251602-phase4-foreach-iterator-ref-log.md` | Log: ForeachDriver requires second param as `ValueAst.Reference` (iterator name from ref); FlowTests + FlowErrorTests `invalid_type`; 709 tests; worktree note. | ForeachDriver, iterator, Reference, invalid_type |
| `2603251602-phase4-foreach-iterator-ref-plan.md` | Plan: bind `/foreach` iterator from reference AST, not resolved string; fatal on non-reference; fix `*it` / “Variable name must be a string”; tests. | foreach, ValueAst.Reference, phase4 |
| `2603251602-phase4-foreach-iterator-ref-walkthrough.md` | Post-merge walkthrough: `/foreach` iterator must be `ValueAst.Reference` (`*name`); non-reference → fatal `invalid_type`; `ForeachDriver` + `FlowTests`/`FlowErrorTests`; 710 tests. | foreach, ForeachDriver, iterator, reference, Phase4, walkthrough |
| `2603251603-phase4-flowutils-breakif-verb-plan.md` | Closed plan: `breakif`/`continueif` must execute resolved `ZohVerb` and use returned value for truthiness in `FlowUtils`; tests in `FlowTests`. | breakif, continueif, FlowUtils, verb condition, Phase4, plan |
| `2603251604-phase4-do-returned-verb-plan.md` | Closed plan: when first `/do` argument returns a `ZohVerb`, execute it once more (single extra hop); `DoDriver` + `ControlFlowVerbsTests`. | do, DoDriver, returned verb, two-hop, Phase4, plan |
| `2603251810-phase4-do-returned-verb-patch.md` | Patch walkthrough: `DoDriver` second `ExecuteVerb` on `ZohVerb` result; suspend/fatal from first hop preserved; new test; 711 tests. | DoDriver, patch, suspend, fatal, Phase4 |
| `2603251825-phase4-flowutils-breakif-verb-patch.md` | Patch: shared condition resolution executes verb then `IsTruthy` for `breakif`/`continueif`; `Loop_BreakIfVerb_*` / `Foreach_ContinueIfVerb_*`; 713 tests. | FlowUtils, breakif, continueif, patch, Phase4 |
| `2603252130-phase4-condition-suspend-fatal-impl-log.md` | Execution log: `EvaluateBreakIf`/`EvaluateContinueIf`/`EvaluateCondition`; loop/sequence/foreach/while propagate suspend/fatal; five `FlowTests`; 719 tests. | suspend, fatal, FlowUtils, WhileDriver, execution log |
| `2603252130-phase4-condition-suspend-fatal-impl-plan.md` | Closed impl plan: stop collapsing verb conditions via `.ValueOrNothing`—propagate `Suspend`/`IsFatal` like `IfDriver` from `FlowUtils` and `WhileDriver` condition verbs. | suspend, fatal, condition verb, WhileDriver, Phase4, plan |
| `2603252130-phase4-condition-suspend-fatal-impl-walkthrough.md` | Walkthrough: suspend/fatal propagation for `breakif`/`continueif` and `/while` conditions; `EvaluateCondition` vs planned `ResolveConditionValue` naming; csharp repo root note; 719 tests. | walkthrough, suspend, fatal, Phase4 |
| `2603252220-phase4-sequence-breakif-verb-test-patch.md` | Patch: `Sequence_BreakIfVerb_UsesReturnedBoolean` mirrors loop coverage for `SequenceDriver` + `FlowUtils`; closes memo/umbrella gap; 714 tests. | sequence, breakif, FlowTests, patch, Phase4 |
| `2603261500-phase5-concurrency-context-signal-gaps-fix-log.md` | Execution log: `/jump` and `/fork` variadic trailing `*var` transfer (CallDriver-style parse); snapshot before cross-story exit; six tests; 725 tests. | jump, fork, variable transfer, JumpDriver, ForkDriver, Phase5, log |
| `2603261500-phase5-concurrency-context-signal-gaps-fix-walkthrough.md` | Walkthrough: variadic ref transfer on `/jump`/`/fork`, `TryGetWithScope`/`Set`, contract validation order; optional shared helper skipped; 725 tests. | jump, fork, transfer, NavigationTests, ConcurrencyTests, Phase5, walkthrough |

---

## Notes

- Most work follows **plan → log → walkthrough** (or **plan → patch**) with rising test counts recorded in later documents.
- **Phase 4** control-flow items are split across `/if`, `/switch`, `/foreach`, `breakif`/`continueif`, `/do`, sequence, and suspend/fatal propagation; **Phase 5** closes with `/jump` and `/fork` variable transfer.
- Several logs note **environment or restore issues** (NU1301, missing `dotnet`) and still record intended or partial verification—full detail remains in git history for those files.
- **`20260226-csharp-archive.md`** was not merged into this index (per archive workflow: existing `*-archive.md` files are not processed) and remains alongside this document until superseded or consolidated manually.
